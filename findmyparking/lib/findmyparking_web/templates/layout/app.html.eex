<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Find My Parking</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
<%= if FindmyparkingWeb.Authentication.load_current_user(@conn) do %>
            <li>Hello <%= FindmyparkingWeb.Authentication.load_current_user(@conn).username %></li>
            <li><%= link "Log out", to: session_path(@conn, :delete, FindmyparkingWeb.Authentication.load_current_user(@conn)), method: "delete" %></li>
<% else %>
            <li><%= link "Log in",  to: session_path(@conn, :new) %></li>
<% end %>
          </ul>
        </nav>
        <h1>Find My Parking Application</h1>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
